[package]
name = "centralize"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "centralize"

[dependencies]
anchor-lang = "1.0.0"

[features]
idl-build = ["anchor-lang/idl-build"]